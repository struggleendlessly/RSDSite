@{
    @if (ServiceItems.Count > 0)
    {
        var length = ServiceItems.Count;
        if (ServiceItems.Count > 3)
        {
            length = 4;
        }

        <div class="row row-cols-1 row-cols-md-2 g-5 py-5">

            @for (int i = 0; i < length; i++)
            {
                var item = ServiceItems[i];
                var url = item.LongDesc.FirstOrDefault(x => x.Key.Contains(StaticStrings.UrlKeyEnding)).Value;
                var title = item.ShortDesc.FirstOrDefault(x => x.Key.Contains(StaticStrings.TitleKeyEnding)).Value;
                var subtitle = item.ShortDesc.FirstOrDefault(x => x.Key.Contains(StaticStrings.SubtitleKeyEnding)).Value;
                var href = $"{StateManager.SiteName}/{StateManager.Lang}/{StaticRoutesStrings.ServicePageUrl}/{url}";

                <div class="col">
                    <div class="card border-light bg-light h-100">
                        <div class="card-body">
                            <h5 class="card-title">@title</h5>
                            <p class="card-text">@subtitle</p>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
}
