<!-- ========== MAIN CONTENT ========== -->
<main id="content" role="main">
    <!-- Hero -->
    <div class="container content-space-t-4 content-space-b-1 content-space-b-sm-2">
        <div class="w-lg-75">
            @if (Model.Data != null)
            {
                <TinyMceEditor 
                    TinyMceId="@(StaticHtmlStrings.TinyHomeTitleId)"
                    TinyMceContentFormat="@(StaticStrings.TinyContentFormatHtml)"
                    Model="Model"
                    FuncSave="Save"
                    Key="@(StaticStrings.HomePageDataTitleKey)">
                        <h1 class="display-3 mb-5">@((MarkupString) Model.Data[StaticStrings.HomePageDataTitleKey])</h1>
                </TinyMceEditor>
                <TinyMceEditor 
                    TinyMceId="@(StaticHtmlStrings.TinyHomeSubtitleId)"
                    TinyMceContentFormat="@(StaticStrings.TinyContentFormatHtml)"
                    Model="Model"
                    FuncSave="Save"
                    Key="@(StaticStrings.HomePageDataSubtitleKey)">
                        <p class="lead">@((MarkupString) Model.Data[StaticStrings.HomePageDataSubtitleKey])</p>
                </TinyMceEditor>
            }
        </div>
    </div>
    <!-- End Hero -->

    <div class="border-top mx-auto" style="max-width: 25rem;"></div>

    <!-- Card Grid -->
    <div class="container content-space-2 content-space-lg-3">
        <!-- Heading -->
        <div class="w-md-65 mb-5 mb-md-9">
            @if (Model.Data != null)
            {
                <TinyMceEditor 
                    TinyMceId="@(StaticHtmlStrings.TinyHomeFeaturedCaseStudiesTitleId)"
                    TinyMceContentFormat="@(StaticStrings.TinyContentFormatHtml)"
                    Model="Model"
                    FuncSave="Save"
                    Key="@(StaticStrings.HomePageDataFeaturedCaseStudiesTitleKey)">
                        <span class="text-cap">@((MarkupString) Model.Data[StaticStrings.HomePageDataFeaturedCaseStudiesTitleKey])</span>
                </TinyMceEditor>
                <TinyMceEditor 
                    TinyMceId="@(StaticHtmlStrings.TinyHomeFeaturedCaseStudiesSubtitleId)"
                    TinyMceContentFormat="@(StaticStrings.TinyContentFormatHtml)"
                    Model="Model"
                    FuncSave="Save"
                    Key="@(StaticStrings.HomePageDataFeaturedCaseStudiesSubtitleKey)">
                        <h2 class="lh-base">@((MarkupString) Model.Data[StaticStrings.HomePageDataFeaturedCaseStudiesSubtitleKey])</h2>
                </TinyMceEditor>
            }
        </div>
        <!-- End Heading -->
        
        <FeaturedCaseStudies 
            SiteName="@SiteNameLower"
            ServiceItems="ServiceItems">
        </FeaturedCaseStudies>

        <div class="text-center">
            @if (Model.Data != null)
            {
                <TinyMceEditor 
                    TinyMceId="@(StaticHtmlStrings.TinyHomeViewAllWorksButtonTextId)"
                    TinyMceContentFormat="@(StaticStrings.TinyContentFormatText)"
                    Model="Model"
                    FuncSave="Save"
                    Key="@(StaticStrings.HomePageDataViewAllWorksButtonTextKey)">
                        <a class="btn btn-outline-primary btn-transition" href="https://htmlstream.com/" target="_blank">
                            @(Model.Data[StaticStrings.HomePageDataViewAllWorksButtonTextKey])
                        </a>
                </TinyMceEditor>
            }
        </div>
    </div>
    <!-- End Card Grid -->

    <div class="border-top mx-auto" style="max-width: 25rem;"></div>

    <TestimonialsList
        SiteName="@SiteNameLower">
    </TestimonialsList>

    <div class="border-top mx-auto" style="max-width: 25rem;"></div>
</main>
<!-- ========== END MAIN CONTENT ========== -->
