@using rcl.Components.Pages
@using shared

@attribute [Route(StaticRoutesStrings.ItemsPageRouteWithLang)]
@attribute [Route(StaticRoutesStrings.ItemsPageRouteWithSitenameAndLang)]
@attribute [Route(StaticRoutesStrings.PortfolioPageRouteWithSitenameAndLang)]

@rendermode InteractiveServer

@if (GetPageType() == shared.Enums.ServicesPageType.Services)
{
        <CascadingValue Value="theme">
            <ServicesComponent PageType="shared.Enums.ServicesPageType.Services">
            </ServicesComponent>
    </CascadingValue>

}
else
{
           <CascadingValue Value="theme">
            <ServicesComponent PageType="shared.Enums.ServicesPageType.Services">
            </ServicesComponent>
    </CascadingValue>
}