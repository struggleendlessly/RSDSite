@using rcl.Components.Pages
@using shared

@attribute [Route(StaticRoutesStrings.PricingPageRoute)]
@attribute [Route(StaticRoutesStrings.PricingPageRouteWithParameter)]

@rendermode InteractiveServer

<PricingComponent></PricingComponent>
@code {
    [Parameter]
    public string? siteName { get; set; }
}

